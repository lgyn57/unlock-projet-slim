<DOCTYPE! html>
<html>
    <head>
        <title>My First Web Page</title>
    </head>

    <body>
        <h1>Test</h1>

        {% for card in cards %}
            <p>{{card.id}}</p>
        {% endfor %}
        
    </body>
</html>
